#!/usr/bin/python3
def element_at(my_list, idx):
    a = len(my_list)
    for i in my_list:
        if idx < a:
            return none
        elif idx > a:
            return none
        else:
            return (my_list(i))
